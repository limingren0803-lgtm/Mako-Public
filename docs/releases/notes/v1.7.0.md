# Mako v1.7.0 发布说明

发布日期：2026-08-11

## 版本概览

Mako v1.7.0 完善了官方职位情报的审核、时效和维护流程。职位数据仍由受控流程导入，聊天请求只读取本地已经核验的记录，不在回答过程中访问企业官网。

## 职位审核

新职位和发生变化的职位版本先进入待审核状态。待审核数据不会替换当前有效版本，也不会进入 CareerAgent 检索。审核通过后切换当前版本；拒绝时继续使用此前已经核验的职位。

相同内容再次核验时只更新时间，不产生重复版本。发生变化时保留版本历史和审核结果。

## 用户选择的时效范围

职位根据最近核验时间和官网有效期标记为 `fresh`、`aging`、`stale` 或 `expired`。

`/chat` 增加可选字段 `job_max_age_days`，求职用户可以为当前请求选择 1–90 天的职位核验窗口，默认值为 30。扩大范围时，系统会显示职位的最近核验时间和时效状态；expired 职位始终排除。

该选择只作用于当前请求，不写入 CareerProfile 或 Memory。响应会回显 `job_max_age_days`，便于客户端展示当前使用的数据范围。

## 来源健康与受控刷新

官方来源记录最近检查、成功和失败时间、连续失败次数及错误类型。需要更新时，由管理员明确创建并运行持久化刷新任务；任务限制重试次数，并能恢复因进程中断而停留在运行状态的记录。

刷新 URL 继续限制在已登记的 HTTPS 官方域名。该版本不增加定时调度、后台批量抓取或绕过登录和验证码的行为。

## 兼容性

以下内容保持不变：

- GeneralAgent、TechnicalAgent、CareerAgent 和六个 Career Skills；
- CareerProfile 字段、提取、合并和跨会话复用；
- Redis working-memory keys 和 ChromaDB collection 名称；
- 现有 Docker volume 名称及挂载位置；
- `/chat`、`/monitor`、`/debug/profile` 等既有 API 路径。

SQLite 使用增量迁移保存审核、时效、来源健康和刷新任务数据，既有来源和职位记录可以继续使用。

## 验证

46/46 公开基线回归、Python 编译检查、Docker Compose 配置校验、生产镜像构建和服务健康检查均已通过。

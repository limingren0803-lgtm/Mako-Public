# Mako v1.5.0 发布说明

发布日期：2026-08-10

## 版本概览

Mako v1.5.0 在职业知识管理基础上增加官方职位情报管线。职位数据经过来源验证、结构化、版本管理和生命周期控制后，可供岗位匹配和 JD 分析使用。六个 Career Skills、CareerProfile Schema、Memory 行为和现有 API 路径保持不变。

## 职位数据与生命周期

- `JobPosting` 统一记录来源标识、企业、职位名称、地点、职责、要求、招聘类型、发布时间、有效期、更新时间和官方链接；
- 职位内容使用 SHA-256 哈希识别变化，重复内容沿用现有版本，发生有效变化时保留历史并启用新版本；
- 职位状态分为 `active`、`inactive` 和 `expired`，到达官网声明的有效期后记录会过期，但历史版本不会删除；
- 只有经过确认的完整快照才能停用本次未出现的职位，不完整列表和空解析结果不会触发批量停用；
- 刷新结果和职位写入结果纳入现有监控指标。

## 官方来源与安全边界

- 职位刷新沿用 v1.4 的来源登记和审批边界，只接受已登记的 HTTPS 官方域名；
- 获取过程继续检查 DNS、实际连接地址、跳转目标、robots 规则、响应类型和响应大小；
- 适配器与存储层分别校验来源身份、企业名称和职位链接域名；
- 聊天请求只读取本地已验证职位，不会在回答过程中访问招聘网站；
- 无法从公开页面可靠解析职位时，刷新会保留现有记录，不尝试绕过登录、验证码、访问限制或调用未经确认的动态接口。

## 首批来源状态

- 百度官方招聘列表已完成服务端渲染数据映射。验证页面返回 10 条唯一职位，官网同时报告总量为 150 条，因此该结果按不完整快照处理，不执行缺失职位停用；
- 腾讯、华为、字节跳动和美团的官方公开页面已完成首轮结构核验。当前页面依赖动态加载，或未在公开 HTML 中提供可验证的职位列表，本版本不对这些来源进行自动解析；
- 后续接入以官方公开页面或经过验证的官方数据访问方式为前提，不使用社交平台和来源不明的数据。

## CareerAgent 接入

- `career_match` 和 `career_jd` 请求可以从本地登记库检索 `active` 且未过期的职位；
- 检索使用确定性排序并限制返回数量，技术问题、普通问答和其他 Career 意图不会注入职位上下文；
- 提供给 Agent 的职位上下文包含企业、职位、地点、职责、要求、来源更新时间和官方链接，并标记为外部事实背景；
- `/chat` 响应增加 `job_data_used` 和 `job_sources`，调用方可以确认本次回答是否使用职位数据及其来源。

## 验证结果

- 完整确定性回归测试 108/108 通过；
- Docker Compose 配置校验和生产镜像构建通过，镜像内 108/108 回归通过；
- ChromaDB 服务端和客户端匿名遥测均关闭，候选容器启动日志无错误；
- 使用 `pip-audit==2.10.1` 完成依赖审计，未发现已知漏洞；
- 五个 Compose 服务均为 healthy，容器重启次数为 0；
- 应用直连、Nginx、ChromaDB 和 Prometheus 在线检查均返回 HTTP 200；
- 职位登记库通过独立临时 volume 的跨容器恢复检查，现有持久化数据未被修改。

## 已知边界

- 百度列表的完整分页方式尚未确认，本版本不会把单页结果作为完整职位快照；
- 腾讯、华为、字节跳动和美团尚无可验证的自动职位刷新能力；
- 职位信息反映最近一次成功更新时的官网状态，用户准备投递前仍需通过返回的官方链接确认职位是否有效。

## 兼容性

以下内容保持不变：

- GeneralAgent、TechnicalAgent、CareerAgent 和六个 Career Skills；
- CareerProfile 字段、schema-guided extraction、conservative merge 和跨会话复用；
- Redis working-memory keys 和 ChromaDB collection 名称；
- 现有 Docker volume 名称及其挂载目标；
- `/chat`、`/monitor`、`/debug/profile` 等现有 API 路径。
